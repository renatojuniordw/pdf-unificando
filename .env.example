# Copie este arquivo para `.env` e ajuste os valores.
# Nunca versionar o `.env` real (ver .gitignore).

# --- Público (embutido no bundle do cliente via build-args do Docker) ---
NEXT_PUBLIC_GA_ID=G-WDL8Q73DPM
NEXT_PUBLIC_GTM_ID=GTM-5FJHKG2C
NEXT_PUBLIC_META_PIXEL_ID=1491713342606274
NEXT_PUBLIC_ADSENSE_ID=ca-pub-6897422992813570
NEXT_PUBLIC_PRIVACY_EMAIL=privacidade@unificando.com.br

# --- Servidor ---
SITE_URL=https://pdf.unificando.com.br
MAX_FILE_SIZE=52428800
MAX_UPLOAD_FILES=20
MAX_TOTAL_UPLOAD_BYTES=52428800
MAX_CONCURRENT_JOBS=2
MAX_QUEUE_SIZE=5
RETRY_AFTER_SECONDS=30
ALLOWED_ORIGIN=https://pdf.unificando.com.br

# Se definida, exige Authorization: Bearer <chave> nas rotas /api/pdf/*
# API_SECRET_KEY=