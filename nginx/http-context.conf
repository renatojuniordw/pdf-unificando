# Inclua este arquivo no bloco http {} do seu nginx.conf principal:
#   include /etc/nginx/conf.d/http-context.conf;
#
# Zona de rate limiting para uploads PDF (10MB de memória ≈ 160k IPs)
limit_req_zone $binary_remote_addr zone=pdf_upload:10m rate=5r/m;
limit_req_status 429;
